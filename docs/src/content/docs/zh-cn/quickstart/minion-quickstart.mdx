---
title: 随从快速入门
description: 从一个最小随从开始，创建召唤牌、行动、强化牌和绑定随从的牌。
sidebar:
    order: 2
---

import {Aside, Tabs, TabItem} from '@astrojs/starlight/components';

本指南会实现一个最小随从玩法：

1. 创建一个随从模型。
2. 写一张召唤随从的牌。
3. 给随从添加一个可点击的行动。
4. 写一张以随从为目标的强化牌。
5. 写一张绑定某个随从的衍生牌。

示例项目可以对照仓库中的 `MinionLib.Example/Minions`、`MinionLib.Example/Cards`、`MinionLib.Example/Actions` 和 `MinionLib.Example/Powers`。

## 选择你的基类

如果你的项目没有使用 BaseLib 或 RitsuLib，直接继承 MinionLib 原生类。使用 BaseLib 或 RitsuLib 时，先按[环境配置](/MinionLib/zh-cn/quickstart/environment/)安装对应依赖，之后继承生成器提供的适配器类。

<Tabs syncKey="adapter">
    <TabItem label="Vanilla">

        ```csharp
        using MinionLib.Action;
        using MinionLib.Component;
        using MinionLib.Component.Core;
        using MinionLib.Minion;

        public abstract class MyMinionBase : MinionModel;

        public abstract class MyCardBase(
        int cost,
        CardType type,
        CardRarity rarity,
        TargetType target)
        : ComponentsCardModel(cost, type, rarity, target);

        public abstract class MyActionBase : ActionModel;
        ```

    </TabItem>
    <TabItem label="BaseLib">

        ```csharp
        using MyCoolMod.BaseLibAdapters;

        public abstract class MyMinionBase : CustomMinionModel;

        public abstract class MyCardBase(
        int cost,
        CardType type,
        CardRarity rarity,
        TargetType target)
        : CustomComponentsCardModel(cost, type, rarity, target);

        public abstract class MyActionBase : CustomActionModel;
        ```

    </TabItem>
    <TabItem label="RitsuLib">

        ```csharp
        using MyCoolMod.RitsuAdapters;

        public abstract class MyMinionBase : ModMinionTemplate;

        public abstract class MyCardBase(
        int cost,
        CardType type,
        CardRarity rarity,
        TargetType target)
        : ModComponentsCardTemplate(cost, type, rarity, target);

        public abstract class MyActionBase : ModActionTemplate;
        ```

    </TabItem>
</Tabs>

<Aside type="caution" title="替换命名空间">
    上面的 `MyCoolMod` 是示例程序集名。实际代码中请替换成你的程序集命名空间，例如 `{你的程序集名}.BaseLibAdapters` 或
    `{你的程序集名}.RitsuAdapters`。
</Aside>

## 创建一个随从

随从本质上是特殊的怪物模型。最小实现通常需要视觉资源路径、初始生命值，以及召唤时的初始化逻辑。

<Tabs syncKey="adapter">
    <TabItem label="Vanilla">

        ```csharp
        using MegaCrit.Sts2.Core.Commands;
        using MegaCrit.Sts2.Core.Entities.Creatures;
        using MegaCrit.Sts2.Core.Entities.Players;
        using MegaCrit.Sts2.Core.GameActions.Multiplayer;
        using MegaCrit.Sts2.Core.Models;
        using MegaCrit.Sts2.Core.Powers;
        using MinionLib.Minion;
        using MinionLib.Powers;

        public sealed class MyMinion : MinionModel
        {
            public override int MinInitialHp => 6;
            public override int MaxInitialHp => 6;
            protected override string VisualsPath =>
            "res://Example/MinionTest/scenes/creature_visuals/pettest_attackaka.tscn";

            public override async Task OnSummon(
            PlayerChoiceContext choiceContext,
            Player owner,
            MinionSummonOptions options)
        {
            var self = owner.PlayerCombatState?.Pets.FirstOrDefault(p => ReferenceEquals(p.Monster, this));
            if (self == null) return;

            if (options.MaxHp is decimal maxHp)
            await CreatureCmd.SetMaxAndCurrentHp(self, maxHp);

            if (options.PrimaryStatAmount is decimal strength && strength > 0m)
            await PowerCmd.Apply<StrengthPower>(self, strength, owner.Creature, options.Source);

            await PowerCmd.Apply<MinionGuardianPower>(self, 1m, owner.Creature, options.Source);
        }
        }
        ```

    </TabItem>
    <TabItem label="BaseLib">

        ```csharp
        using MegaCrit.Sts2.Core.Commands;
        using MegaCrit.Sts2.Core.Entities.Creatures;
        using MegaCrit.Sts2.Core.Entities.Players;
        using MegaCrit.Sts2.Core.GameActions.Multiplayer;
        using MegaCrit.Sts2.Core.Models;
        using MegaCrit.Sts2.Core.Powers;
        using MinionLib.Minion;
        using MinionLib.Powers;
        using MyCoolMod.BaseLibAdapters;

        public sealed class MyMinion : CustomMinionModel
        {
            public override int MinInitialHp => 6;
            public override int MaxInitialHp => 6;
            public override string? CustomVisualPath =>
            "res://Example/MinionTest/scenes/creature_visuals/pettest_attackaka.tscn";

            public override async Task OnSummon(
            PlayerChoiceContext choiceContext,
            Player owner,
            MinionSummonOptions options)
        {
            var self = owner.PlayerCombatState?.Pets.FirstOrDefault(p => ReferenceEquals(p.Monster, this));
            if (self == null) return;

            if (options.MaxHp is decimal maxHp)
            await CreatureCmd.SetMaxAndCurrentHp(self, maxHp);

            if (options.PrimaryStatAmount is decimal strength && strength > 0m)
            await PowerCmd.Apply<StrengthPower>(self, strength, owner.Creature, options.Source);

            await PowerCmd.Apply<MinionGuardianPower>(self, 1m, owner.Creature, options.Source);
        }
        }
        ```

    </TabItem>
    <TabItem label="RitsuLib">

        ```csharp
        using MegaCrit.Sts2.Core.Commands;
        using MegaCrit.Sts2.Core.Entities.Creatures;
        using MegaCrit.Sts2.Core.Entities.Players;
        using MegaCrit.Sts2.Core.GameActions.Multiplayer;
        using MegaCrit.Sts2.Core.Models;
        using MegaCrit.Sts2.Core.Powers;
        using MinionLib.Minion;
        using MinionLib.Powers;
        using MyCoolMod.RitsuAdapters;

        public sealed class MyMinion : ModMinionTemplate
        {
            public override int MinInitialHp => 6;
            public override int MaxInitialHp => 6;
            public override string? CustomVisualsPath =>
            "res://Example/MinionTest/scenes/creature_visuals/pettest_attackaka.tscn";

            public override async Task OnSummon(
            PlayerChoiceContext choiceContext,
            Player owner,
            MinionSummonOptions options)
        {
            var self = owner.PlayerCombatState?.Pets.FirstOrDefault(p => ReferenceEquals(p.Monster, this));
            if (self == null) return;

            if (options.MaxHp is decimal maxHp)
            await CreatureCmd.SetMaxAndCurrentHp(self, maxHp);

            if (options.PrimaryStatAmount is decimal strength && strength > 0m)
            await PowerCmd.Apply<StrengthPower>(self, strength, owner.Creature, options.Source);

            await PowerCmd.Apply<MinionGuardianPower>(self, 1m, owner.Creature, options.Source);
        }
        }
        ```

    </TabItem>
</Tabs>

<Aside type="note" title="解析随从实体">
    `OnSummon` 的 `this` 是模型对象。需要操作战斗中的随从时，先从 `owner.PlayerCombatState.Pets` 中找到 `Monster` 引用当前模型的
    `Creature`。
</Aside>

## 创建召唤牌

召唤随从推荐使用`MinionCmd.AddMinion<T>()`。`MinionSummonOptions` 可以传入生命值、主要数值、来源和站位。

<Tabs syncKey="adapter">
    <TabItem label="Vanilla">

        ```csharp
        using MegaCrit.Sts2.Core.Entities.Cards;
        using MegaCrit.Sts2.Core.GameActions.Multiplayer;
        using MinionLib.Component;
        using MinionLib.Component.Core;
        using MinionLib.Commands;
        using MinionLib.Minion;

        public sealed class SummonMyMinionCard()
        : ComponentsCardModel(1, CardType.Power, CardRarity.Rare, TargetType.Self)
        {
            protected override async Task OnPlay(
            PlayerChoiceContext choiceContext,
            CardPlay cardPlay,
            ComponentContext componentContext)
        {
            _ = await MinionCmd.AddMinion<MyMinion>(choiceContext, Owner, new MinionSummonOptions(
            MaxHp: 8m,
            PrimaryStatAmount: 2m,
            Source: this,
            Position: MinionPosition.Front));
        }

            protected override void OnUpgrade(ComponentContext componentContext) {}
        }
        ```

    </TabItem>
    <TabItem label="BaseLib">

        ```csharp
        using MegaCrit.Sts2.Core.Entities.Cards;
        using MegaCrit.Sts2.Core.GameActions.Multiplayer;
        using MinionLib.Component.Core;
        using MinionLib.Commands;
        using MinionLib.Minion;
        using MyCoolMod.BaseLibAdapters;

        public sealed class SummonMyMinionCard()
        : CustomComponentsCardModel(1, CardType.Power, CardRarity.Rare, TargetType.Self)
        {
            protected override async Task OnPlay(
            PlayerChoiceContext choiceContext,
            CardPlay cardPlay,
            ComponentContext componentContext)
        {
            _ = await MinionCmd.AddMinion<MyMinion>(choiceContext, Owner, new MinionSummonOptions(
            MaxHp: 8m,
            PrimaryStatAmount: 2m,
            Source: this,
            Position: MinionPosition.Front));
        }

            protected override void OnUpgrade(ComponentContext componentContext) {}
        }
        ```

    </TabItem>
    <TabItem label="RitsuLib">

        ```csharp
        using MegaCrit.Sts2.Core.Entities.Cards;
        using MegaCrit.Sts2.Core.GameActions.Multiplayer;
        using MinionLib.Component.Core;
        using MinionLib.Commands;
        using MinionLib.Minion;
        using MyCoolMod.RitsuAdapters;

        public sealed class SummonMyMinionCard()
        : ModComponentsCardTemplate(1, CardType.Power, CardRarity.Rare, TargetType.Self)
        {
            protected override async Task OnPlay(
            PlayerChoiceContext choiceContext,
            CardPlay cardPlay,
            ComponentContext componentContext)
        {
            _ = await MinionCmd.AddMinion<MyMinion>(choiceContext, Owner, new MinionSummonOptions(
            MaxHp: 8m,
            PrimaryStatAmount: 2m,
            Source: this,
            Position: MinionPosition.Front));
        }

            protected override void OnUpgrade(ComponentContext componentContext) {}
        }
        ```

    </TabItem>
</Tabs>

## 创建可点击行动

Action 是一种可以点击触发的能力。把它应用到随从身上后，玩家可以点击随从或能力图标来执行它。

<Tabs syncKey="adapter">
    <TabItem label="Vanilla">

        ```csharp
        using MegaCrit.Sts2.Core.Commands;
        using MegaCrit.Sts2.Core.Entities.Creatures;
        using MegaCrit.Sts2.Core.GameActions.Multiplayer;
        using MegaCrit.Sts2.Core.Powers;
        using MegaCrit.Sts2.Core.ValueProps;
        using MinionLib.Action;
        using MinionLib.Commands;

        public sealed class MyAttackAction : ActionModel
        {
            public override TargetType TargetType => TargetType.AnyEnemy;
            public override bool AutoRemoveAtTurnEnd => true;
            public override PowerType Type => PowerType.Buff;
            public override PowerStackType StackType => PowerStackType.Counter;

            protected override async Task OnAct(
            PlayerChoiceContext choiceContext,
            Creature actor,
            Creature? target)
        {
            if (target == null) return;

            await MinionAnimCmd.PlayBumpAttackAsync(actor, target);
            await CreatureCmd.Damage(choiceContext, target, 4m, ValueProp.Move, actor, null);
        }
        }
        ```

    </TabItem>
    <TabItem label="BaseLib">

        ```csharp
        using MegaCrit.Sts2.Core.Commands;
        using MegaCrit.Sts2.Core.Entities.Creatures;
        using MegaCrit.Sts2.Core.GameActions.Multiplayer;
        using MegaCrit.Sts2.Core.Powers;
        using MegaCrit.Sts2.Core.ValueProps;
        using MinionLib.Commands;
        using MyCoolMod.BaseLibAdapters;

        public sealed class MyAttackAction : CustomActionModel
        {
            public override TargetType TargetType => TargetType.AnyEnemy;
            public override bool AutoRemoveAtTurnEnd => true;
            public override PowerType Type => PowerType.Buff;
            public override PowerStackType StackType => PowerStackType.Counter;

            protected override async Task OnAct(
            PlayerChoiceContext choiceContext,
            Creature actor,
            Creature? target)
        {
            if (target == null) return;

            await MinionAnimCmd.PlayBumpAttackAsync(actor, target);
            await CreatureCmd.Damage(choiceContext, target, 4m, ValueProp.Move, actor, null);
        }
        }
        ```

    </TabItem>
    <TabItem label="RitsuLib">

        ```csharp
        using MegaCrit.Sts2.Core.Commands;
        using MegaCrit.Sts2.Core.Entities.Creatures;
        using MegaCrit.Sts2.Core.GameActions.Multiplayer;
        using MegaCrit.Sts2.Core.Powers;
        using MegaCrit.Sts2.Core.ValueProps;
        using MinionLib.Commands;
        using MyCoolMod.RitsuAdapters;

        public sealed class MyAttackAction : ModActionTemplate
        {
            public override TargetType TargetType => TargetType.AnyEnemy;
            public override bool AutoRemoveAtTurnEnd => true;
            public override PowerType Type => PowerType.Buff;
            public override PowerStackType StackType => PowerStackType.Counter;

            protected override async Task OnAct(
            PlayerChoiceContext choiceContext,
            Creature actor,
            Creature? target)
        {
            if (target == null) return;

            await MinionAnimCmd.PlayBumpAttackAsync(actor, target);
            await CreatureCmd.Damage(choiceContext, target, 4m, ValueProp.Move, actor, null);
        }
        }
        ```

    </TabItem>
</Tabs>

可以在随从的`OnSummon` 中解析出`self` 后应用行动：

```csharp
await PowerCmd.Apply<MyAttackAction>(self, 1m, owner.Creature, options.Source);
```

## 以随从为目标

MinionLib 提供`MinionTargetTypes`，可以直接用于卡牌目标。

```csharp
using MegaCrit.Sts2.Core.Commands;
using MegaCrit.Sts2.Core.Entities.Cards;
using MegaCrit.Sts2.Core.GameActions.Multiplayer;
using MegaCrit.Sts2.Core.Powers;
using MinionLib.Component;
using MinionLib.Component.Core;
using MinionLib.Minion;
using MinionLib.Targeting;

public sealed class EmpowerMinionCard()
    : ComponentsCardModel(1, CardType.Skill, CardRarity.Uncommon, MinionTargetTypes.AnyMinion)
{
    protected override async Task OnPlay(
        PlayerChoiceContext choiceContext,
        CardPlay cardPlay,
        ComponentContext componentContext)
    {
        if (cardPlay.Target is not { Monster: MinionModel } target) return;

        await PowerCmd.Apply<StrengthPower>(target, 2m, Owner.Creature, this);
        await PowerCmd.Apply<DexterityPower>(target, 2m, Owner.Creature, this);
    }
}
```

常用目标类型在`MinionLib.Targeting.MinionTargetTypes` 中，例如`AnyMinion`、`AnyMinionOrOwner`、`AllMinions` 和`AllCreatures`。

## 创建绑定随从的牌

此功能正在重构，暂不可用；替代方案是手动保存生物的`CombatId`，需要时再从战斗状态中取回对应`Creature`。

## Guardian 与站位

内置`MinionGuardianPower` 可以让随从替玩家阻挡伤害：

```csharp
await PowerCmd.Apply<MinionGuardianPower>(self, 1m, owner.Creature, options.Source);
```

召唤时可以指定站位：

```csharp
public enum MinionPosition
{
    Front,
    Back,
    FrontUpper,
    BackUpper,
    Upper
}
```

站位会影响视觉位置，也会影响部分和随从顺序相关的机制。
